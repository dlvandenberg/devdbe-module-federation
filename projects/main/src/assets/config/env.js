(function (window) {
    window.__env = window.__env || { };

    // MFE Module URLs
    window.__env.modules = {
        mfe2: {
            remoteEntry: 'http://localhost:4202/remoteEntry.js',
            remoteName: 'mfe2'
        }
    }
}(this));